import { accounts, campaigns } from '../../../../lib/demo-data'; import { fail, ok } from '../../../../lib/api';
import { canAccessMcc } from '../../../../lib/data-access'; import { PERMISSIONS } from '../../../../lib/permissions'; import { requirePermission } from '../../../../lib/rbac';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const access=await requirePermission(PERMISSIONS.VIEW_CAMPAIGN);if(access.error)return access.error;const {id}=await params;const item=campaigns.find(x=>x.id===id);if(!item)return fail('NOT_FOUND','Không tìm thấy chiến dịch.',404);const account=accounts.find(value=>value.id===item.accountId);if(account&&!await canAccessMcc(access.user,account.mccId))return fail('FORBIDDEN','Bạn không có quyền xem chiến dịch này.',403);return ok(item)}
